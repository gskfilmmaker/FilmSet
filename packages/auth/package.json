{
  "name": "@filmset/auth",
  "version": "0.1.0",
  "private": true,
  "description": "RBAC, project isolation, and session boundary. Established now per the security-from-V1 requirement; full policy implementation deferred until after the FRAME prototype phase is approved (see build brief §0, Product Definition security section).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.6.3"
  }
}
